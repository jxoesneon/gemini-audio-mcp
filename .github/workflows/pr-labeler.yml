name: Labeler

on:
  pull_request:
    branches: [ master, main ]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull_requests: write
    steps:
      - uses: actions/labeler@v5
